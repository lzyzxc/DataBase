@model IEnumerable<MySuperMarket.Models.EXPENSE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EXPENSE_DATE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MONEY)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TYPE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EXPENSE_DATE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MONEY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TYPE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EXPENSE_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EXPENSE_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EXPENSE_ID })
        </td>
    </tr>
}

</table>

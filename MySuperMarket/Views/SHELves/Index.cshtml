@model IEnumerable<MySuperMarket.Models.SHELF>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SHELF_AREA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SHELF_AREA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SHELF_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SHELF_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SHELF_ID })
        </td>
    </tr>
}

</table>

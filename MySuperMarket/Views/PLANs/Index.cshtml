@model IEnumerable<MySuperMarket.Models.PLAN>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PLAN_NUM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRODUCT_ATTRIBUTE.SUPPLIER_ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PLAN_NUM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRODUCT_ATTRIBUTE.SUPPLIER_ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PLAN_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PLAN_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PLAN_ID })
        </td>
    </tr>
}

</table>
